

===================================================================================

void Solve(int arr[], int len,int idx=0) {
	if (idx == len-1)
		return;

	Solve(arr,len, idx +1);
	arr[idx] = max(arr[idx], arr[idx+1]);

}



===================================================================================or