#include<queue>  // for queue , priority_queue
//  زى الكيو  بس فيها اولوية يعنى مترتبين من  الصغير للكبير او العكس 
// مش  بقدر اوصل لعنصر فى النص  بمشى من الاول للاخر 
/*
	priority_queue
 
	decleration
	push
	pop
	top   //
	size
	empty
 
	*/
-------------------------------------------------------------------------------------------

	priority_queue<int> p1;    // بترتب من  الكبير للصغير  
	priority_queue<int, vector<int>, greater<int> > p2; // بترتب من الصغير للكبير تاكد
 
	p1.push(1); // 1 2 3 
 
	p1.push(3);
 
	p1.push(2); // log(n) // عشان بترتب
 
 
	p1.pop(); // log(n)  /////<-------------من المميزات 
 
 
	p1.top(); // o(1) // اصغر عنصر او اكبر عنصر حسب انت بترتب ازاى  الى  هو فى الاول خاالص 
 
	cout << p1.empty() << endl;
 
	cout << p1.size() << endl;
 
	while (!p1.empty()){
		cout << p1.top() << endl;
		p1.pop();
	}

--------------------------------------------------------------------------------------
car2 = priority_queue<int>(); //لو عاوز تفضى  الحاج  بسرعة
--------------------------------------------------------------------------------------
car2 = car1;        // ينفع تساويهم 

 